import 'package:flutter/material.dart';

class PlanetScreen extends StatelessWidget {
  const PlanetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Planetas");
  }
}
