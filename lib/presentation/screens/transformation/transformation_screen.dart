import 'package:flutter/material.dart';

class TransformationScreen extends StatelessWidget {
  const TransformationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Transformaciones");
  }
}
